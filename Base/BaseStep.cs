namespace SeleniumFramework.Base
{
    /// <summary>BaseStep will extend by all the step classes, The common methods related to step classes will present here </summary>
    public class BaseStep : Base
    {
        public BaseStep(ParallelConfig pc, LoggingStep ls) : base(pc, ls)
        {
        }
    }
}

namespace SeleniumFramework.Base
{
    /// <summary>BasePage will extend by all the page classes, The common methods related to page classes will present here </summary>
    public class BasePage : Base
    {
        public BasePage(ParallelConfig pc, LoggingStep ls) : base(pc, ls)
        {
        }
    }
}
